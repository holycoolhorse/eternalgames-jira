{
  "name": "eternalgames-jira",
  "private": true,
  "version": "1.0.0",
  "description": "EternalGames Jira - Project Management System",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "vercel-build": "npm run build --workspace=frontend"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
